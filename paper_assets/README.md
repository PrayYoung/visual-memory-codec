# Paper 1 release assets

`figure_1_rate_qa.png` is the main rate–QA figure. It is generated from the
frozen canonical aggregate table by
`scripts/generate_paper1_release_artifacts.py`.

`table_1_key_results.csv` is a compact machine-readable transcription of the
predeclared main comparison and the two completed robustness controls. The
underlying tables and protocols are in `results/formal_benchmark/`.
